# Audio Upload Folder

Put your released band tracks in this folder and keep filenames in sync with `script.js`.

Current expected files:
- `audio/lwab-track-01.mp3`
- `audio/lwab-track-02.mp3`
- `audio/lwab-track-03.mp3`

You can rename tracks by editing the `tracks` array in `script.js`.
